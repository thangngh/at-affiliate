# Copyleft

**Copyleft © thangnh** — dự án này được phát hành theo giấy phép copyleft (GNU GPL v3.0
hoặc phiên bản mới hơn). Tự do sử dụng, sao chép, chỉnh sửa và chia sẻ, với điều kiện:
tác phẩm phái sinh phải giữ cùng giấy phép copyleft và giữ nguyên ghi công.

- Tác giả: **thangnh**
- Liên hệ: **thangnh.00@gmail.com**

## Ghi chú về tài nguyên bên thứ ba

- **Không bundle font / ảnh / SVG nào.** Các công cụ web dùng emoji và CSS thuần.
  Video engine (`video_engine.py`) chỉ tham chiếu font hệ thống lúc chạy
  (DejaVuSans / LiberationSans — đều giấy phép tự do OFL/DejaVu; Arial là font hệ
  thống, không phân phối kèm).
- **Package sử dụng đều giấy phép permissive, không copyleft:** edge-tts (MIT),
  moviepy (MIT), imageio-ffmpeg (BSD), pillow (HPND), numpy (BSD). Không có thành
  phần GPL gây nhiễm bản quyền.
- Logo campaign hiển thị trong video được lấy từ Accesstrade lúc chạy, dùng mục
  đích affiliate, không đóng gói vào repo.
